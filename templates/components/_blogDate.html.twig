{% if date(createdAt) > date('-7day') %}
    <small class="text-secondary">
        {{ createdAt|date('M j, Y') }}
    </small>
{% else %}
    <small class="text-secondary">- {{ createdAt|date('M j, Y') }}
    </small>
{% endif %}
